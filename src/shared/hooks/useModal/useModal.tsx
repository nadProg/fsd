type UseModalOptions = {
  onClose?: () => void;
  isOpen?: boolean;
  lazy?: boolean;
  animationDelay?: number;
};

export const useModal = ({
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  animationDelay,
  lazy,
  isOpen,
  onClose,
}: UseModalOptions) => null;
